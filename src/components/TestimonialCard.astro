---
interface Props {
  name: string;
  role: string;
  testimonial: string;
  rating: number;
}

const { name, role, testimonial, rating } = Astro.props;
---

<div class="card">
  <div class="flex items-center gap-1 mb-4">
    {Array.from({ length: 5 }).map((_, i) => (
      <svg
        class:list={['w-4 h-4', i < rating ? 'text-yellow-400' : 'text-gray-200']}
        fill="currentColor"
        viewBox="0 0 20 20"
      >
        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
      </svg>
    ))}
  </div>
  <p class="text-gray-600 text-sm leading-relaxed mb-6 italic">
    "{testimonial}"
  </p>
  <div class="flex items-center gap-3">
    <div class="w-10 h-10 bg-primary-100 rounded-full flex items-center justify-center">
      <span class="text-primary-600 font-semibold text-sm">{name[0]}</span>
    </div>
    <div>
      <p class="text-sm font-semibold text-gray-900">{name}</p>
      <p class="text-xs text-gray-500">{role}</p>
    </div>
  </div>
</div>
